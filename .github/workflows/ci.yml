name: ci
on:
  push:
    branches: [ "main", "sprint2/**" ]
  pull_request:
    branches: [ "main" ]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "18"
      - run: echo "No tests yet — Sprint 2 scaffold"
